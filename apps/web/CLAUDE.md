# apps/web — Graflare frontend

TanStack Start (SSR on Cloudflare Workers) + React Compiler. Calls the API Worker through the
`API` service binding (RPC), never over HTTP. UI components come from `@graflare/ui`.
