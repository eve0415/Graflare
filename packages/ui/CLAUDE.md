# packages/ui — Graflare design system

shadcn/ui components, source-only (apps import the `.tsx` directly; no build step). Built on
**Base UI** (`@base-ui/react`), not Radix; style **Rhea**, base color **Mauve** (OKLCH). This
distinct look is deliberate — don't swap it for default zinc/Radix.
